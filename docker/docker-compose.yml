services:
  octybot-demo:
    build: .
    container_name: octybot-demo
    stdin_open: true
    tty: true
    volumes:
      - wrangler-auth:/root/.wrangler
      - claude-auth:/root/.claude
      - octybot-repo:/root/octybot

volumes:
  wrangler-auth:
  claude-auth:
  octybot-repo:
