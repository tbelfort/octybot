{
  "name": "claude-pa-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create claude-pa",
    "d1:migrate": "wrangler d1 execute claude-pa --file=src/schema.sql --remote",
    "d1:migrate:local": "wrangler d1 execute claude-pa --file=src/schema.sql --local"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.63.0"
  }
}
